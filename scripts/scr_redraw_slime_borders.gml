var tempsurf = surface_create(CELLSIZE, CELLSIZE);

surface_set_target(tempsurf);
//draw_clear_alpha( c_black, 0 );
    

// check slime block above
if!(position_meeting(x - CELLSIZE, y - CELLSIZE, o_slime)) {
   // apply mask
          
}
